[project]
name = "django-map-libre"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=5.0",
    "djangorestframework>=3.18.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "python-dotenv>=1.2.3",
]
